{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksConfig": {
      "providers": ["python"]
    }
  },
  "deploy": {
    "startCommand": "pwd && ls -la && echo '--- Looking for main.py ---' && find . -name '*.py' -type f && echo '--- Trying to start ---' && python main.py 2>/dev/null || echo 'main.py not found at root'",
    "restartPolicyType": "ON_FAILURE"
  }
}